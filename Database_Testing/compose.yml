services:
  postgres:
    image: postgres:18-alpine
    container_name: crm_db_psgr
    ports:
      - 8080:5432
    environment:
      - POSTGRES_DB=crm 
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    networks:
      - dbnet
    volumes:
      - postgres-data:/var/lib/postgresql/data

networks:
  dbnet:
    driver: bridge

volumes:
  postgres-data:
    